import React, {useState, useEffect} from 'react';
import { StyleSheet, SafeAreaView,  FlatList, Text, View } from 'react-native';
import Parse from 'parse/react-native';

export default MovieCompose = () => {
    return (
        <SafeAreaView>
            <Text>Movie Compose</Text>
        </SafeAreaView>
    )
}